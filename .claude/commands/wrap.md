---
description: Session wrap-protocol checklist per CLAUDE.md
allowed-tools: Bash(.claude/hooks/wrap-check.sh:*)
---

!.claude/hooks/wrap-check.sh

Based on the checklist above:

- For each `[ ]` unchecked item, name the specific next step needed to close it.
- If every item is `[x]` and the branch is ahead of main, surface: "Ready to wrap — run the 6-step protocol."
- If blockers remain, list them compactly (no more than one line per blocker).

Non-blocking self-check: do not take actions; just report. The user decides whether to proceed with wrap.
