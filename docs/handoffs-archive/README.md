# HANDOFFs — historical archive

Sessions 2-17 retros archived here at session-24 wrap to declutter `docs/` top-level.

**Why archive, not delete:** handoffs document decisions + the *why* behind them. Archived means still version-controlled + greppable, just not in the top-level `docs/` listing where they compete for attention with live specs.

**Retrieval:** `rg "<term>" docs/handoffs-archive/` works identically to top-level search.

**What's still in `docs/`:** latest five HANDOFFs (sessions 18, 20-23) remain top-level for quick reference during active work. Note session 19's retro was not produced (jump straight to 20 was intentional at the time).

**Archive threshold:** when `docs/HANDOFF-SESSION-*.md` exceeds ~7 files, archive the oldest back into this folder.
