# S-PROTO-sign-up — loop progress

Target: `acceptance.md` §OBJECTIVE. Base: `main` @ `2e35ca3`. Branch: `claude/trusting-brahmagupta-uFE21`.

## Rounds

| Round | Change | Verifier evidence | Critic's largest gap | Next action |
|---|---|---|---|---|
| 0 | Bars authored (`tests/e2e/sign-up.{journey,visual-bar}.e2e.ts`, `playwright.config.ts`, Chromium pinned to the sandbox build); shell untouched | Journey bar RED 6/7 against the 49-line shell — structure, validation, hand-off, keyboard all absent; the shell also overflows horizontally at 375px. A11y floor passes (near-empty page). Visual bar captures both sides bare at 402×874 (canvas: `M_SignUp` mounted standalone — the editor's device bezel and pan/zoom viewport were a blind-pick tell; the artboard `id` never reaches the DOM, slots are `[data-dc-slot]`). Canvas served over http (CORS blocked its state-JSON fetch on `file://`), Tailwind CDN stubbed, app's Inter shared via route stub. Thumbnail eyeballed: real screens both sides. Inter-loaded asserted on both captures; EnvBanner (dev banner) hidden in the rendered capture — dev chrome was a tell | — | Start the loop (gated on go + token cap) |
| 1 | Round 1: built /dev/proto/sign-up from canvas M_SignUp (L3667-3727) — TopBar with back-arrow Link to pre-signup-interview + "Have an account?", Wordmark (WORDMARK from @/constants, green dot), serif "Start your case." h1 + lede, 3-step Acco | journey 7/7; axe true; lint true; tsc true; blind pick A (spotted impl) minor; realDOM true | [axe color-contrast, impact=serious, 1 node, wcag2aa/143] Fires only AFTER an invalid submit, so the load-state scan (0 violations across 89 rules) misses it. Target `.sign-up-module__72puSW__alert` (`<p role="alert">`): fg #FF3B30 (`--ds-c | Round 2: fix largest gap |
| 2 | Round 2 fixed the after-submit axe color-contrast finding: `.alert` in sign-up.module.css now uses `color-mix(in srgb, var(--ds-color-danger) 65%, var(--ds-color-ink))` (resolves to #AF2F28, 6.44:1 on white — AA at any size, no inline hex,  | journey 7/7; axe true; lint true; tsc true; blind pick B (spotted impl) minor; realDOM true | /home/user/construct_d_01/src/app/dev/proto/sign-up/page.tsx:186 hard-codes the brand: `I agree to Decouple&apos;s <span className={styles.termsLink}>Terms</span>` — while L6 imports `WORDMARK` and /home/user/construct_d_01/src/constants/in | Round 3: fix largest gap |
| 3 | Round 3 closed the largest gap: the brand literal is gone from the sign-up screen. `src/app/dev/proto/sign-up/page.tsx` now imports `APP_NAME` from `@/constants`; the terms label reads `I agree to {APP_NAME}&apos;s …` (possessive needs the  | journey 7/7; axe true; lint true; tsc true; blind pick A (spotted impl) minor; realDOM true | Visual minor: Stepper layout: in A the '2 About you' step sits centred between steps 1 and 3 (label at x~180-265), whereas in B it is pulled ~25px left (x~157-242) with unequal connector lengths; combined with the ~3px taller field boxes, t | Integration critic |

## Failed approaches

(none yet)

## Escalations

- **Serif token never resolves.** `layout.tsx` loads `Source_Serif_4` via next/font under its own CSS variable, but `--ds-font-serif` / `tokens.font.serif` hardcode `'Source Serif Pro'` — a different family name — so every serif heading in the prototype renders in Georgia (verified with `document.fonts` on `/dev/proto/sign-up`: only `Inter 400` loaded). Design-system fix (tokens.ts + globals.css, parity-tested), outside this loop's edit scope. Serif parity with the canvas holds today only because the canvas also falls back to Georgia in the sandbox.
- **Inter is registered at weight 400 only** (90 `@font-face` rules on `/dev/proto/sign-up`, all `font-weight: 400` unicode-range subsets). Every `fontWeight: 500/600/700` in the prototype is browser-synthesised faux bold. Fix belongs with the serif token fix (next/font `Inter` weights or the variable font in `layout.tsx`), outside this loop's scope. Parity with the canvas capture holds because both sides synthesise from the same 400 face.
- Sans parity for the visual bar: app has self-hosted Inter; the canvas cannot reach Google Fonts from headless Chromium. Capture spec serves the app's Inter to the canvas via a route stub so the blind pick isn't decided by font-family.
- Token drift (SUB · MUTE · LINE · ACCENT · AI-card purples) — raised to the user only if the visual critic's largest gap lands on it. See `acceptance.md` §Drift rule.
- "Pay" stepper label vs free-tier copy on the same screen — logged as an open question, not routed.
- r1 DRIFT: /home/user/construct_d_01/src/app/dev/proto/sign-up/sign-up.module.css:83-85 `.wordmarkDot { color: var(--ds-color-phase-finalise) }` (#166534, tokens.ts L40) stands in for drift-table row ACCENT `#2F6D5F` marked "no token" (acceptance.md L28). No hex was inlined (correct), but `phase-finalise` is the Phase-5 identity colour from the 68g phase colour system, not a brand accent — the pre-auth brand mark now carries Finalise-phase semantics, and forest #166534 is a visibly different green from the canvas's muted teal #2F6D5F.
- r1 DRIFT: /home/user/construct_d_01/src/app/dev/proto/sign-up/sign-up.module.css:261-292 — the AI trust card draws from two unrelated token families plus a fabricated colour: `.aiCard` background `--ds-color-phase-build-soft` (#EEF2FF) and border `color-mix(in srgb, var(--ds-color-phase-build) 18%, var(--ds-color-surface-panel))` (L264); `.aiTitle` colour `--ds-color-phase-build` (#4338CA, L291); `.aiBadge` background `--ds-color-accent-violet` (#7C3AED, L275). Drift-table "AI card" row (acceptance.md L29): canvas #4C3FB8 / #F5F3FF / #E4DEFD, "no token". The color-mix() border is a value outside the palette — the same class of bypass as an inline hex, just computed.
- r2 DRIFT: /home/user/construct_d_01/src/app/dev/proto/sign-up/sign-up.module.css:83-85 `.wordmarkDot { color: var(--ds-color-phase-finalise); }` (#166534, tokens.ts:40) stands in for canvas `ACCENT="#2F6D5F"` (Standalone.html:1352; drift table acceptance.md:28 'no token'). No hex was inlined — correct — but `phase-finalise` is the Phase-5 identity colour of the 68g phase colour system, so the pre-auth brand mark is painted in Finalise's colour. Already recorded as r1 DRIFT at docs/slices/S-PROTO-sign-up/progress.md:23; still in the code, still unresolved. No sibling surface shares the treatment: pre-signup-interview/components/BrandBar.tsx:7-17 renders `{WORDMARK}` in ink with no dot; welcome-tour/page.tsx:136-149 renders a glyph + lowercase `decouple` in `#111` — the green dot exists only on this screen.
- r2 DRIFT: /home/user/construct_d_01/src/app/dev/proto/sign-up/sign-up.module.css:264-295 builds the AI trust card from two token families plus a derived colour: `.aiCard` `background: var(--ds-color-phase-build-soft)` (#EEF2FF, L266) and `border: 1px solid color-mix(in srgb, var(--ds-color-phase-build) 18%, var(--ds-color-surface-panel))` (L267); `.aiTitle` `color: var(--ds-color-phase-build)` (#4338CA, L294); `.aiBadge` `background: var(--ds-color-accent-violet)` (#7C3AED, L278). Canvas M_SignUp (Standalone.html:3667-3730) + AIBadge (L2293): `background:"#F5F3FF"`, `border: 1px solid #E4DEFD`, `color:"#4C3FB8"`, badge `AI_PURPLE` — drift table acceptance.md:29 'no token'. Build-phase indigo makes the card read as phase chrome on a pre-phase screen, and the violet badge does not match its own card. Recorded as r1 DRIFT at progress.md:24; unresolved.
- r2 DRIFT: canvas `SUB="#5F6368"`, `MUTE="#9AA0A6"`, `LINE="#EAECEF"` (Standalone.html:1351) are rendered with the warm-stone tokens per drift-table rows acceptance.md:25-27 — `--ds-color-text-sub` at sign-up.module.css L64, L100, L177, L221, L300; `--ds-color-text-muted` at L110, L198, L212; `--ds-color-border` at L38, L135, L158, L188. Token side used throughout — correct handling. The cool-vs-warm gap is the standing escalation at progress.md:21.
- r3 DRIFT: AI card border is a synthesised colour chasing the drift-table 'AI card' row (acceptance.md L29: #E4DEFD border, 'no token'). /home/user/construct_d_01/src/app/dev/proto/sign-up/sign-up.module.css L267: `border: 1px solid color-mix(in srgb, var(--ds-color-phase-build) 18%, var(--ds-color-surface-panel));` resolves to ~#DDDBF5, a colour that exists nowhere in tokens.ts/globals.css. Mixing two tokens to land next to a canvas hex is the same act as inlining the hex — the output is an off-catalogue colour chosen to match the canvas.
- r3 DRIFT: AI card palette borrows two unrelated semantic tokens to approximate canvas purple #4C3FB8 / #F5F3FF (acceptance.md L29 names nearest as accent.violet, 'no token'). sign-up.module.css L266 `.aiCard background: var(--ds-color-phase-build-soft)` (#EEF2FF), L294 `.aiTitle color: var(--ds-color-phase-build)` (#4338CA), L278 `.aiBadge background: var(--ds-color-accent-violet)` (#7C3AED). `phase.build` means the Build phase (tokens.ts L36-41, 68g C-V1 phase colour system); sign-up is Start phase, and the card now carries two hues (indigo text/surface, violet badge).
- r3 DRIFT: wordmark dot uses the Finalise-phase green as a stand-in for canvas ACCENT #2F6D5F (acceptance.md L28: 'no token'). sign-up.module.css L83-84 `.wordmarkDot { color: var(--ds-color-phase-finalise); }` (#166534). /home/user/construct_d_01/src/app/dev/proto/sign-up/page.tsx L56-63 splits the wordmark into `{APP_NAME}` + `<span className={styles.wordmarkDot}>.</span>` purely to colour the dot. The existing header renders the wordmark uncoloured (/home/user/construct_d_01/src/app/dev/proto/_components/ProtoHeader.tsx L65-71).

## Boundaries remaining

Rounds: 3/6 · Wall-clock: ~60 min · Same-gap-twice: 0/2
