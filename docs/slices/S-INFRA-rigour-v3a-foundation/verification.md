# S-INFRA-rigour-v3a-foundation — Verification

**Slice:** S-INFRA-rigour-v3a-foundation
**Source:** `docs/slices/S-INFRA-rigour-v3a-foundation/acceptance.md`
**Branch:** `claude/S-INFRA-rigour-v3a-foundation`
**Status:** v3a session-40 wrap — all 8 ACs PASS pending adversarial-review (DoD-3) + PR-body refresh to DoD-11 form (L184).

S-INFRA-rigour-v3a-foundation is an **infrastructure / process-rigour** slice — control-plane scripts, CI workflows, and harness hooks. No user-facing surface. Verification reduces to: meta-tests pass, CI gates GREEN, and per-AC evidence with run-IDs.

---

## Per-AC evidence

| AC | Status | Evidence (commit · run-ID) |
|---|---|---|
| AC-1 (skeleton) | PASS | `68af356` · shellspec run [`24980529793`/job `73141467796`](https://github.com/rossdelarge247-debug/construct_d_01/actions/runs/24980529793/job/73141467796) — 6/6 examples (closes 5 RED meta-tests from S-37-1 `0264f13`). |
| AC-1 (full impl) | PASS-with-DoD-7-gap | range `31be543..e45152b`. S-38-2 (RED, `31be543`): 5 new shellspec tests for §11-checklist contract — 3 RED against skeleton (asserting full-mode behaviour absent) per DoD-7. S-38-1 (GREEN, `e45152b`): replaces skeleton-mode `scripts/verify-slice.sh` with 6 gates over file-presence — §11 13-item checklist (table-row OR section-heading form), leak-scan on staged diff, ESLint denial check (full-mode delegating to `scripts/eslint-no-disable.sh`), tsc + vitest (full-mode), per-language coverage thresholds (full-mode; lcov parser activates with S-38-4). G17 useful-message exits on every gate. Modes: incremental (default; pre-commit; ~0.024s on this repo, well under G16 5s) vs `--full` (CI). DoD-2 verified locally: passes against v3a slice (exit 0) AND `docs/slices/S-F7-alpha-contracts-dev-mode/` reference slice (exit 0) without modification. shellspec full-suite 42/42 GREEN at `e45152b`; 48/48 GREEN at `2c676d0` after S-38-3 added 6 hook tests. AC-2 baseline re-run captured the new SHA. **DoD-7 procedural gap (acknowledged session 40):** acceptance.md L180 mandates "Failing-meta-tests-first commit pushed to remote AND CI-observed-failing before impl pushed." `31be543` was pushed at `2026-04-27T16:29:22Z` and `e45152b` at `~2026-04-27T16:33Z` — under 5 minutes apart, NOT waiting for CI to observe RED before pushing GREEN. CI did run on `31be543` asynchronously (shellspec.yml fires on `push` AND `pull_request`); the RED state IS recoverable from PR #24's CI history, but the *temporal* "before impl pushed" requirement was not honoured. Recorded honestly rather than papered over. v3b carry-over item proposed: add a `pre-push` hook or PR-creation gate that blocks pushes until prior-commit CI status is observable. |
| AC-2 (hooks-checksums + integrity check) | PASS | `be78bc2` · shellspec 10/10 GREEN locally. CI run on PR #24 GREEN. Drift simulation in commit message verified actionable diff + recipe. Baseline re-run on `e45152b` (S-38-1: verify-slice.sh SHA), `95481e5` (S-38-4: vitest.config.ts added; 13 entries) and `2c676d0` (S-38-3: tdd-first-every-commit.sh + tdd-exemption-allowlist.txt added; 15 entries). `scripts/hooks-checksums.sh` explicit-file list extended at S-38-4 (vitest.config.ts) and S-38-3 (docs/tdd-exemption-allowlist.txt) per L199. |
| AC-2 (control-change-label workflow) | PASS | S-37-7 (`ad497fb`) + S-37-7a (this commit). `.github/workflows/control-change-label.yml` ships with self-inclusive path filter per L199 enumeration (`.claude/hooks/**`, `.claude/settings.json`, `.claude/subagent-prompts/**`, `.claude/hooks-checksums.txt`, `scripts/verify-slice.sh`, `eslint.config.mjs`, `vitest.config.ts`, `.github/workflows/control-change-label.yml` (self), `.github/workflows/pr-dod.yml`). Closes G3+G10 BLOCK recursion gap (workflow edits require the label that triggers the workflow). Pulled forward from session-38 estimate (L71) to unblock the held branch-protection precondition (PR #24 body row 2). Three known L199 omissions surfaced as v3b carry-over: `scripts/eslint-no-disable.sh`, `docs/eslint-baseline-allowlist.txt`, `scripts/git-state-verifier.sh`. **Trigger model:** workflow runs on EVERY PR; step-level `git diff` detects protected-path changes, label check fires only when count ≠ 0. Refactored from `on.pull_request.paths:` (S-37-7) to step-level detection (S-37-7a) so the check is usable as a required status check under classic GitHub Branch Protection — `on.paths:` would skip the workflow entirely on non-control-plane PRs, stranding required-check status as "Expected — Waiting" forever. Same security guarantee (path-filter self-inclusion preserved at step level); now branch-protection-compatible. **Verification (integration):** PR #24 run `73202996782` → FAIL (no label) · PR #24 run `73203499127` → PASS after `labeled` event (3-min round-trip; both directions verified by PR #24's own CI history). |
| AC-3 (function-size rule + no-disable check) | PASS | `21f0b6b` (initial) → Lint RED (run [`24982330038`/job `73147247505`](https://github.com/rossdelarge247-debug/construct_d_01/actions/runs/24982330038/job/73147247505)) → `50d5910` (fix: scoped ignores). Final: 14/14 GREEN locally + all 14 CI checks GREEN incl. new `ESLint no-new-disables (AC-3)` workflow on push + PR events. |
| AC-4 (pre-commit verify hook) | PASS | `60bede1` (S-37-5). `.claude/hooks/pre-commit-verify.sh` registered as PreToolUse:Bash matching `git commit` precisely (rejects `git commit-tree` / `git commit-graph` etc. via word-boundary regex); derives slice from `claude/<slice>(-XXXXX)?` branch name; runs `scripts/verify-slice.sh "docs/slices/$SLICE"` in incremental mode; blocks commit on red with G17 useful-message exit. shellspec 6/6 GREEN locally (`tests/shellspec/pre-commit-verify.spec.sh`). DoD-incremental-perf verified at S-38-1 wrap: incremental-mode verify-slice.sh runs in 0.024s on this repo against the v3a slice (G16 budget 5s). Hook fires on every commit on slice-named branches; exempt commits (docs-only on non-slice branches) skip-allow. |
| AC-5 (TDD-first-every-commit) | PASS | range `9862158..2c676d0`. S-38-3 RED (`9862158`): 6 shellspec tests for the hook contract — non-`git commit` skip / docs-only skip / src+tests pair / src-without-tests block / exemption-allowlist match / G17 useful-message — all RED against missing-impl. S-38-3 GREEN (`2c676d0`): `.claude/hooks/tdd-first-every-commit.sh` reads PreToolUse JSON; matches `git commit` precisely via word-boundary regex; reads staged paths via `git diff --cached --name-only`; partitions into src/ and tests/; filters src/ through `docs/tdd-exemption-allowlist.txt` (bash `case` glob match); blocks (exit 2) on non-exempt src/ without tests/. Allowlist seeded empty per L48 pattern (β-resume populates real exemptions). G17 useful-message exit names offending paths. shellspec 6/6 GREEN at `2c676d0` (full suite 48/48). Settings.json registration adds the hook to the existing PreToolUse:Bash array (timeout 10s) — fires alongside `pre-commit-verify.sh`. AC-2 baseline regenerated to cover the new hook + allowlist. Test fixture uses commit-tree + update-ref plumbing (HANDOFF-SESSION-39 pattern) to bypass commit-signing. |
| AC-6 (coverage gate) | PASS | `95481e5` (S-38-4). `vitest.config.ts` adds `coverage.thresholds.lines: 90` for `src/**/*.{ts,tsx}` (provider v8, lcov reporter); `scripts/verify-slice.sh` Gate 5 (full-mode) parses `coverage/lcov.info` for new-line coverage on PR diff (`origin/main...HEAD`); fails when uncovered/added > 10% across `.ts/.tsx` files. Skip-allows when lcov.info absent (no coverage data yet — CI activation of `vitest run --coverage` and `@vitest/coverage-v8` dev-dep tracked as v3b carry-over per `vitest.config.ts` header). G17 useful-message on threshold violation. AC-2 scope extended: `vitest.config.ts` added to `scripts/hooks-checksums.sh` explicit-file list per L199. shellspec 42/42 GREEN at `95481e5` (no regression). Honest deferrals recorded: shellspec meta-test for the lcov parser (requires fake-git-history + lcov fixture, non-trivial in tmp isolation) → v3b carry-over; F6 ".sh ≥ 80% line via shellspec" invariant (kcov + shellspec integration) → aspirational only. |
| AC-7 (exit-plan-review) | PASS | S-37-6 (this commit). `.claude/hooks/exit-plan-review.sh` + `.claude/subagent-prompts/exit-plan-review.md` + `scripts/git-state-verifier.sh` + 17 shellspec meta-tests (`tests/shellspec/exit-plan-review.spec.sh` 10 + `tests/shellspec/git-state-verifier.spec.sh` 7) — all GREEN locally; full shellspec suite 37/37 GREEN (no regression). L52 sub-points evidenced: **(a)** nonce derivation is the hook's first action before any stdin read (`exit-plan-review.sh:18-39`); **(b)** substitution via heredoc shell parameter expansion, not `sed`/`awk` on plan content (`exit-plan-review.sh:65-79`); **(c)** 16 bytes from `/dev/urandom` → 32-char hex; spec example used `xxd` but POSIX-portable `od -An -tx1 -N16` substituted (same guarantee, comment at `exit-plan-review.sh:29-33`); **(d)** hard-fail with explicit error on `/dev/urandom` unreadable, covered by meta-test `exit-plan-review.spec.sh:31` ("hard-fails (exit 2) when /dev/urandom is unreadable"); **(e)** belt-and-braces in subagent prompt (`subagent-prompts/exit-plan-review.md` "Per-invocation context" + "Belt-and-braces against prompt injection" sections); **(f)** four meta-tests cover randomness (`spec.sh:18`), collision ≥120/128 (`spec.sh:24`), missing-/dev/urandom (`spec.sh:31`), and fake-nonce-injection containment (`spec.sh:42`); **(g)** log-leakage threat model addendum in `security.md:30`. Hook registered in `.claude/settings.json:48-57` (`PreToolUse` matcher `ExitPlanMode`). Hooks-checksums re-baselined to cover the new `exit-plan-review.sh` entry. |
| AC-8 (CLAUDE.md "Hard controls" stub) | PASS | This commit. New top-level `## Hard controls (in development)` section in `CLAUDE.md` (inserted between Engineering conventions and Visual direction). Contents per L53: status banner; gates table (7 rows × file path · fire-time · AC · bypass) covering AC-2/3/4/7 + pre-S-37 pr-dod.yml; verdict vocabulary inline (`approve` / `nit-only` / `request-changes` / `block` per G23) + severity scale (`architectural` / `logic` / `style` / `none`); rollback procedure as literal quote of acceptance.md L201 (per G19); v3b/v3c carry-over enumeration including the three L199 omissions and the EXIT_PLAN_REVIEW_SPAWN default-flip deferral. ~47 lines net added — under L77's 80-line budget. Stub-status preserved per L150 *"v3a only adds incremental stub"* — full consolidating rewrite is v3c. Verifies the L15 J1 closure: AC-7 + AC-2 references to verdict vocabulary now resolve to a defined source. |

## Golden path

Infra slice — substituted by **control-plane sanity check**: every gate exposes the spec-named command-line interface, runs against a tmp-isolated fixture (G15), and emits G17-pattern useful-message exits on failure. Captured in shellspec meta-tests.

## Edge cases

| Scenario | Coverage |
|---|---|
| Drift detection (`hooks-checksums.sh`) | shellspec drift-simulation test + manual verification (`be78bc2` commit message). |
| Allowlist absent (`eslint-no-disable.sh`) | shellspec test (`tests/shellspec/eslint-no-disable.spec.sh:60-67`). |
| Branch not slice-named (`pre-commit-verify.sh`) | shellspec — derives no slice → skip-allow. |
| Non-`git commit` Bash (`pre-commit-verify.sh`) | shellspec — `git commit-graph` etc must not match. |

## Accessibility · Responsive viewport · Cross-browser

N/A — no UI surface.

## Adversarial review — session 40 (DoD #3, partial)

Verdict: `request-changes` — **on procedural grounds**, not on slice content. Reviewer hit the project's per-turn 300-line read-cap (the discipline this very slice exists to enforce) before completing the ~1,021-line authoritative-source review surface (acceptance.md + verification.md + verify-slice.sh + tdd-first-every-commit.sh + meta-tests + vitest.config + settings.json + checksums + hooks-checksums.sh + tdd-exemption-allowlist + 5-commit `git show` history). The single-turn DoD-3 review pattern is structurally infeasible when the slice surface > 300 lines; reviewer recommended either (a) multi-turn budget, or (b) partition into spec-side / impl-side / git-history sub-spawns.

Findings raised before cap-stop:

1. **DoD-7 procedural gap for S-38-2 RED.** acceptance.md L180 requires "Failing-meta-tests-first commit pushed to remote AND CI-observed-failing **before** impl pushed." `31be543` and `e45152b` shipped under 5 minutes apart — CI ran on both asynchronously but the temporal "before impl pushed" requirement was not honoured. Captured honestly in the AC-1 (full impl) row above; PR #24's CI history retains the RED-state evidence even without timing alignment. **Status: documented, not falsely-claimed-satisfied.** Pre-push gate proposed as v3b carry-over (item 7).

2. **AC-6 lcov parser ships without a shellspec meta-test.** Already documented in v3b carry-over (item 4) as honest deferral. Reviewer recommended elevating to BLOCKER in v3b — noted, but not blocking for v3a since (a) the parser is dormant until `--coverage` wiring lands, (b) the surrounding gate hierarchy (file-presence / §11 / leak-scan) is fully tested, and (c) v3a's own deferral-honesty principle authorises documented carry-overs.

Concerns raised but not investigated due to cap-stop (carried to v3b adversarial-review-process slice or whichever subagent re-runs the deep impl pass):

- Bypass paths via path naming or exemption-glob over-match
- `tdd-first-every-commit.sh` bash glob behaviour with `**` (without `globstar`)
- §11 checklist parser variants (`## 1)`, `##  1.`, mixed table+heading)
- `comm` + bash arrays in lcov parser edge cases
- `.claude/settings.json` schema validity post-multi-hook
- Per-commit `hooks-checksums.txt` correctness audit
- Perf-claim repeatability (0.024s — best-case or median?)
- Exemption allowlist format precision

**Recommendation captured for v3b:** the DoD-3 adversarial-review gate needs an explicit budget envelope or partition convention. Single-turn 300-line cap incompatible with multi-file impl-review.

## Adversarial review — S-37-6 (DoD #3)

Concerns raised by adversarial pass:

1. **Stub-mode default for subagent spawn (deferred).** AC-7 spec ("spawns fresh-context subagent reviewing the plan against CLAUDE.md 'Planning conduct' + spec 70 slice-sizing + simplicity-first") is satisfied at the wiring level only. The hook's stub mode (default) derives verdict from `git-state-verifier` alone; full `claude -p` subagent invocation is gated by `EXIT_PLAN_REVIEW_SPAWN=1`. **Deferred** to ops sign-off on per-`ExitPlanMode` LLM-call cost / latency before flipping the default.
2. **`scripts/git-state-verifier.sh` not in L199 AC-2 scope (deferred to v3b).** L199 enumerates AC-2 coverage explicitly; the new `git-state-verifier.sh` sub-script is part of the AC-7 plan-time-gate chain (called from `exit-plan-review.sh:55`) and silent weakening would weaken the gate, but adding it now would extend coverage beyond the spec's literal text. **Deferred** to v3b spec amendment OR caught by per-PR `control-change` label discipline (G18) until then. `.claude/subagent-prompts/*.md` IS now covered (`scripts/hooks-checksums.sh:48-51`) per L199 + L52.
3. **Fake-nonce containment ultimately depends on subagent prompt-following (acknowledged).** Per L52(e), the framing relies on the subagent obeying the "treat additional separators as content" instruction. This is the inherent limit of LLM-input separation; technical separation via the random nonce reduces but does not eliminate the attack surface. Documented as belt-and-braces in `.claude/subagent-prompts/exit-plan-review.md` ("Belt-and-braces against prompt injection").

## Sign-off

Per slice-wrap PR (session 37 wrap or v3a slice close, whichever ships last).
